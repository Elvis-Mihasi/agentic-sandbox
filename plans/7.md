# Plan for #7: Add readiness and liveness probes

## Goal
Run two application replicas and add Kubernetes readiness and liveness probes
against `GET /health`. Keep the application endpoint, service routing, and
runtime configuration unchanged.

## Dependencies (confirmed / plausible / open), each with change: yes | no
- **Confirmed — `k8s/deployment.yaml` (`spec.replicas`, container): yes:**
  currently has one replica and no probes; add `replicas: 2` plus HTTP probes.
- **Confirmed — `src/app.ts` (`GET /health`): no:** already provides the
  required health response on the HTTP port.
- **Confirmed — `k8s/service.yaml` (no):** already selects all `app=sandbox-app`
  pods and targets the named HTTP port.
- **Confirmed — `.github/workflows/ci.yml` (no):** rollout, stability, smoke,
  and browser gates already validate the deployment; do not alter the pipeline.
- **Confirmed — `src/config.ts`, `k8s/configmap.yaml`, `README.md` (no):**
  probes add no environment variables or contract changes.
- **Confirmed — `test/app.test.ts` and `e2e/smoke.spec.ts` (no):** the health
  endpoint is already covered and no application behavior changes.
- **Plausible — probe timing values (yes):** use repository-compatible HTTP
  probes on port 3000 and `/health`; exact delays/periods should be kept
  conservative for the small app and verified by the pipeline rollout.
- **Open — none.**

## Tests
- No source or test changes expected.
- Run `npm run verify` for regression coverage.
- Push and inspect pipeline rollout, two-pod readiness/stability, smoke, and
  browser gates because Kubernetes manifests are verified in CI.

## Environment contract
No variables are added or changed. `src/config.ts`, `k8s/configmap.yaml`, and
the README environment table remain synchronized.

## Risks
- Probes pointed at the wrong port/path or with overly aggressive timing can
  prevent rollout or cause restarts.
- Two replicas increase resource demand, so requests and limits must remain
  unchanged and both pods must schedule in kind.
- Pipeline risks are rollout, stability, smoke, and browser gates; unit,
  lint, and build behavior should remain unchanged.

## Open questions
None.

## Decisions
